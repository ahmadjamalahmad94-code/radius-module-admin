Flask==3.1.2
Flask-SQLAlchemy==3.1.1
psycopg[binary]>=3.2,<4
python-dotenv==1.2.1
pytest==9.0.1
cryptography>=42,<46
google-auth>=2.30,<3
google-auth-oauthlib>=1.2,<2
google-api-python-client>=2.130,<3
# Central FCM push sender (services/firebase_fcm.py). Optional at runtime: the
# sender lazily imports it and disables itself (graceful no-op) when the package
# OR the uploaded service-account credential is absent. The ONE global mobile
# app is backed by a single central Firebase project owned by this panel.
firebase-admin>=6.5,<7
psutil>=5.9,<8
alembic>=1.13,<2
